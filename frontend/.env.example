# Keep this relative for local development. Next.js proxies /api/v1 to the backend,
# so the browser does not hit localhost/127.0.0.1 CORS mismatches.
NEXT_PUBLIC_API_URL=/api/v1
NOTIFYHUB_BACKEND_ORIGIN=http://localhost:8080
